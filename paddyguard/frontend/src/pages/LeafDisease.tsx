import React from "react";
const LeafDisease: React.FC = () => (
  <div className="px-4 pt-[calc(env(safe-area-inset-top)+16px)]">
    <h1 className="font-sinhala text-lg font-bold text-forest">
      කොළ රෝග හඳුනාගැනීම — C2
    </h1>
  </div>
);
export default LeafDisease;
